[package]
edition = "2021"
name = "napi-sort-package-json"
version = "0.0.1"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "3.0.0"
napi-derive = "3.0.0"
sort-package-json = "0.0.10"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
codegen-units = 1
strip = "symbols"
